<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Administration</title>
</head>

<body>
  <header class="admin__header">
    <div class="site__header">
      <h1>Administration</h1>
    </div>
  </header>

  <main class="admin__main">
    <aside class="admin__kategorier">
      <h3>
        <a href="/administration">Produkter</a>
      </h3>
      <h3>
        <a href="/categories">Kategorier</a>
      </h3>
      <h3>
      <a href="/">Startsida</a>
      </h3>
    </aside>

    <section class="produkter">
      <table class="admin__table">
        <thead>
          <tr>
            <th>Namn</th>
            <th>Beskrivning</th>
            <th>Märke</th>
            <th>SKU</th>
            <th>Pris</th>
            <th>Publicerad</th>
            <th></th>
          </tr>
        </thead>

        <tbody>
          <% products.forEach(product => { %>
            <tr>
              <td><%= product.type %></td>
              <td><%= product.description %></td>
              <td><%= product.brand %></td>
              <td><%= product.sku %></td>
              <td><%= product.price %> kr</td>
              <td><%= product.created_at %></td>

              <td class="icons">
                <span class="penna">
                  <a href="/administration/products/<%= product.id %>/edit">
                    <img src="/icons/square-pen.svg" alt="Redigera produkt">
                  </a>
                </span>

                <form
                  action="/administration/products/<%= product.id %>/delete"
                  method="POST"
                  class="admin__delete-form"
                >
                  <button type="submit" class="admin__icon-button">
                    <img src="/icons/trash.svg" alt="Ta bort produkt">
                  </button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>

      <a href="/administration/products/new" class="admin__button">
        Lägg till produkt
      </a>
    </section>
  </main>
</body>
</html>